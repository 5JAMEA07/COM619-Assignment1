package com.group.devops.model.user;

/**
 * Enum representing the different roles a user can have in the system.
 */
public enum UserRole {
 /**
  * Role assigned to a regular customer or user of the system.
  */
 CUSTOMER,

 /**
  * Role assigned to a user with administrative privileges in the system.
  */
 ADMINISTRATOR
}
